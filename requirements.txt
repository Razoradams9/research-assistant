# Minimum versions; pip resolves the newest compatible build for your
# Python. Verified importable on Python 3.14 (local dev) and 3.10+ (the
# Ubuntu 22.04 deploy target). Pin exact versions later if you want fully
# reproducible builds on a fixed Python.

# Web framework
fastapi>=0.115.6
uvicorn[standard]>=0.34.0

# Data validation / settings
pydantic>=2.10
pydantic-settings>=2.7

# LLM provider (Groq)
groq>=0.13

# Search providers
tavily-python>=0.5
ddgs>=9.0

# HTTP + env
httpx>=0.28
python-dotenv>=1.0

# Testing
pytest>=8.3
pytest-asyncio>=0.25
