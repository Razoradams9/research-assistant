# Pinned, reproducible dependency set — verified working in development.
# Use this for deployment:  pip install -r requirements.lock.txt
#
# These versions were captured on Python 3.14; all have wheels for
# Python 3.10+ (the Ubuntu 22.04 deploy target). If pip can't resolve a
# pin on the server's Python, fall back to `requirements.txt` (floors).

anyio==4.15.1
ddgs==9.16.0
fastapi==0.141.1
groq==1.7.0
httpx==0.28.1
pydantic==2.13.5
pydantic-core==2.46.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
starlette==1.6.0
tavily-python==0.8.2
uvicorn==0.52.4
